{
  "name": "biztrust",
  "private": true,
  "version": "0.0.0",
  "description": "BizTrust platform repository. Skeleton only: no capability is implemented, secure, compliant or production-ready.",
  "license": "UNLICENSED",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "boundaries:generate": "node scripts/generate-boundary-rules.mjs",
    "boundaries:check": "node scripts/generate-boundary-rules.mjs --check && depcruise --config .dependency-cruiser.cjs modules packages services apps tests",
    "lint:migrations": "node scripts/migration-lint.mjs db/migrations",
    "validate:records": "node scripts/python.mjs scripts/validate_continuity.py",
    "test:boundaries": "node --test \"tests/boundaries/*.test.mjs\"",
    "test": "node --test \"tests/boundaries/*.test.mjs\"",
    "typecheck": "tsc -p tsconfig.json",
    "check:modules": "node scripts/check-module-packages.mjs",
    "verify": "pnpm validate:records && pnpm test:validator && pnpm typecheck && pnpm boundaries:check && pnpm lint:migrations && pnpm check:modules && pnpm test:boundaries && pnpm check:mutations",
    "test:validator": "node scripts/python.mjs -m unittest discover -s tests/unit",
    "check:mutations": "node scripts/mutation-check.mjs"
  },
  "devDependencies": {
    "dependency-cruiser": "16.10.4",
    "typescript": "5.9.3"
  }
}
