// VIOLATES rule 6 from the control plane, which peer review F4 called out by
// name as reachable under the loosened rule.
import { HELPER } from "../../../tests/helpers/src/index.js";
export const TEST_CODE_IN_THE_CONTROL_PLANE = HELPER;
